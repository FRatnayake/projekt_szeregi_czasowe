% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_prognosis.R
\name{create_prognosis}
\alias{create_prognosis}
\alias{plot.prognosis}
\title{Forecast a single sales series with ARIMA and/or Prophet}
\usage{
create_prognosis(
  data,
  series = NULL,
  models = c("arima", "prophet"),
  horizon = 30,
  frequency = c("day", "week", "month"),
  backtest = TRUE,
  test_size = 0.2
)

\method{plot}{prognosis}(x, ...)
}
\arguments{
\item{data}{A sales tibble with \code{date} and \code{sales} (and, for \code{series}
filtering, \code{store_nbr} / \code{family}).}

\item{series}{Optional \code{list(store_nbr = , family = )} selecting one series. If
\code{NULL}, all stores/families are summed into a single aggregate series.}

\item{models}{Character vector of models to fit; subset of
\code{c("arima", "prophet")}.}

\item{horizon}{Number of future periods to forecast.}

\item{frequency}{Aggregation frequency: \code{"day"}, \code{"week"} or \code{"month"}.}

\item{backtest}{Logical; if \code{TRUE}, run a chronological train/test split and
report accuracy metrics.}

\item{test_size}{Fraction of the series held out for the backtest (default
\code{0.2}).}

\item{x}{A \code{prognosis} object.}

\item{...}{Unused; for S3 compatibility.}
}
\value{
An S3 object of class \code{"prognosis"}: a list with \code{forecast} (tidy
tibble of \code{date}, \code{model}, \code{forecast}, \code{lo95}, \code{hi95}), \code{history} (the
prepared series), \code{backtest} (accuracy tibble or \code{NULL}), \code{best_model} and
metadata. Has a \code{\link[=plot]{plot()}} method.
}
\description{
Fits univariate forecasting models to one sales series (a single
\verb{store_nbr x family} series, or the aggregate of everything) and returns tidy
forecasts, optionally with a chronological backtest comparing model accuracy.
}
\details{
\strong{This function is strictly univariate.} Modelling all
54 x 33 = 1782 series of the full panel would be prohibitively expensive
without parallelism, so forecast one series at a time -- e.g. drive it across
a chosen subset with \code{\link[=sales_ts_logic]{sales_ts_logic()}} and a \code{filters}/\code{group_by} argument.
Prophet is optional: if the \pkg{prophet} package is not installed it is
skipped with a warning instead of erroring.
}
\examples{
fc <- create_prognosis(
  sample_sales,
  series = list(store_nbr = 1, family = "BEVERAGES"),
  models = "arima",
  horizon = 14,
  backtest = TRUE
)
fc$backtest
\dontrun{
plot(fc)
}

}
