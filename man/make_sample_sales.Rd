% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_sample_sales.R
\name{make_sample_sales}
\alias{make_sample_sales}
\title{Generate a synthetic retail sales panel matching the Favorita schema}
\usage{
make_sample_sales(
  n_stores = 6,
  n_families = 4,
  start = "2015-01-01",
  days = 365,
  seed = 42
)
}
\arguments{
\item{n_stores}{Number of stores to generate (each gets random \code{type} and
\code{city}/\code{state}).}

\item{n_families}{Number of product families (drawn from the real Favorita
family names).}

\item{start}{Start date (a string or \code{Date}) of the panel.}

\item{days}{Number of consecutive days per series.}

\item{seed}{Random seed for reproducibility.}
}
\value{
A named list with two tibbles:
\describe{
\item{\code{sales}}{Panel with columns \code{id}, \code{date}, \code{store_nbr}, \code{family},
\code{sales}, \code{onpromotion} (schema-compatible with \code{train.csv}).}
\item{\code{stores}}{Store metadata with columns \code{store_nbr}, \code{city}, \code{state},
\code{type}, \code{cluster}.}
}
}
\description{
Produces a reproducible, self-contained sales panel with the exact columns of
the raw \code{train.csv} (\code{id}, \code{date}, \code{store_nbr}, \code{family}, \code{sales},
\code{onpromotion}) plus a separate store-metadata table. The series combine a
linear trend, weekly seasonality, yearly seasonality, a promotion effect and
noise. A handful of data-quality problems are deliberately injected (missing
values, duplicates, negative values, a multi-day gap) so that
\code{\link[=validate_sales_ts]{validate_sales_ts()}} and \code{\link[=clean_sales_ts]{clean_sales_ts()}} have something to detect and fix.
}
\details{
The injected defects are intentional and documented so that the
cleaning pipeline can be demonstrated end to end:
\itemize{
\item several \code{NA} values in \code{sales},
\item 2-3 duplicated \verb{(date, store_nbr, family)} rows,
\item a few negative \code{sales} values,
\item a multi-day gap removed from one series.
}
}
\examples{
sim <- make_sample_sales(n_stores = 3, n_families = 2, days = 90)
head(sim$sales)
sim$stores

}
