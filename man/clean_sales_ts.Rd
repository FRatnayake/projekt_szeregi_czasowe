% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_sales_ts.R
\name{clean_sales_ts}
\alias{clean_sales_ts}
\title{Clean a sales time-series table}
\usage{
clean_sales_ts(
  data,
  missing = c("interpolate", "locf", "zero", "drop"),
  dedupe = c("sum", "mean", "first"),
  sort = TRUE,
  aggregate = NULL
)
}
\arguments{
\item{data}{A sales tibble with \code{date}, \code{store_nbr}, \code{family}, \code{sales},
\code{onpromotion}.}

\item{missing}{How to handle missing \code{sales}: \code{"interpolate"} (linear, via
\code{\link[zoo:na.approx]{zoo::na.approx()}}), \code{"locf"} (last observation carried forward), \code{"zero"}
or \code{"drop"} (remove the rows).}

\item{dedupe}{How to collapse duplicated \verb{(date, store_nbr, family)} rows:
\code{"sum"}, \code{"mean"} or \code{"first"}. \code{onpromotion} is always taken as the maximum
within the duplicate group.}

\item{sort}{Logical; sort by \verb{(store_nbr, family, date)} (default \code{TRUE}).}

\item{aggregate}{Optional \code{"week"} or \code{"month"} to aggregate the series
(sales summed, \code{onpromotion} averaged into a share, \code{is_holiday} reduced
with \code{max} when present).}
}
\value{
A cleaned tibble carrying a \code{"cleaning_log"} attribute (a tibble of
per-step row counts). Retrieve it with \code{attr(result, "cleaning_log")}.
}
\description{
Resolves the data-quality problems detected by \code{\link[=validate_sales_ts]{validate_sales_ts()}}: it fills
or removes missing \code{sales}, collapses duplicated keys, sorts the data and can
optionally aggregate to a coarser frequency. The number of rows affected by
each step is recorded in the \code{"cleaning_log"} attribute of the result.
}
\details{
Steps run in this order: de-duplicate, handle missing values, sort,
and finally aggregate. Interpolation and LOCF are applied \strong{within} each
\verb{store_nbr x family} series after sorting, so values never leak across
series.
}
\examples{
cleaned <- clean_sales_ts(sample_sales, missing = "interpolate", dedupe = "sum")
attr(cleaned, "cleaning_log")

# Aggregate to weekly frequency.
weekly <- clean_sales_ts(sample_sales, aggregate = "week")

}
