% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_sales_ts.R
\name{validate_sales_ts}
\alias{validate_sales_ts}
\alias{summary.sales_validation}
\title{Validate the quality of a sales time-series table}
\usage{
validate_sales_ts(data, value_min = 0, value_max = Inf)

\method{summary}{sales_validation}(object, ...)
}
\arguments{
\item{data}{A sales tibble (e.g. from \code{\link[=load_sales_data]{load_sales_data()}}); must contain the
columns \code{date}, \code{store_nbr}, \code{family}, \code{sales}, \code{onpromotion}.}

\item{value_min}{Lower bound for valid \code{sales} values (default \code{0}).}

\item{value_max}{Upper bound for valid \code{sales} values (default \code{Inf}).}

\item{object}{A \code{sales_validation} object.}

\item{...}{Unused; for S3 compatibility.}
}
\value{
An S3 object of class \code{"sales_validation"}: a list with the issue
counts and supporting tibbles.
}
\description{
Runs a battery of data-quality checks \strong{without modifying the data} and
returns an S3 object of class \code{"sales_validation"} describing what was found.
The object has a Polish-language \code{\link[=print]{print()}} method and a \code{\link[=summary]{summary()}} method
returning a tidy tibble.
}
\details{
The following checks are performed:
\itemize{
\item missing (\code{NA}) values per column,
\item duplicated \verb{(date, store_nbr, family)} keys,
\item \code{sales} values outside \verb{[value_min, value_max]} and negative values,
\item date gaps within each \verb{store_nbr x family} series (frequency consistency),
\item dates in the future (relative to \code{\link[=Sys.Date]{Sys.Date()}}),
\item \code{onpromotion} values outside \verb{\{0, 1, NA\}}.
}
}
\examples{
val <- validate_sales_ts(sample_sales)
val
summary(val)

}
