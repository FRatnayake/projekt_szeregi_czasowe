% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_sales_data.R
\name{load_sales_data}
\alias{load_sales_data}
\title{Load retail sales data from the Favorita CSV files}
\usage{
load_sales_data(
  train_path,
  stores_path = NULL,
  holidays_path = NULL,
  n_max = Inf,
  stores = NULL,
  families = NULL
)
}
\arguments{
\item{train_path}{Path to \code{train.csv}.}

\item{stores_path}{Optional path to \code{stores.csv}. If supplied, store metadata
(\code{city}, \code{state}, \code{type}, \code{cluster}) is left-joined on \code{store_nbr}.}

\item{holidays_path}{Optional path to \code{holidays_events.csv}. If supplied, an
\code{is_holiday} flag and \code{holiday_description} are attached (see \emph{Details}).}

\item{n_max}{Maximum number of rows to read from \code{train.csv} (passed to
\code{\link[readr:read_csv]{readr::read_csv()}}). Handy for fast iteration during development.}

\item{stores}{Optional integer vector of \code{store_nbr} values to keep.}

\item{families}{Optional character vector of \code{family} values to keep.}
}
\value{
A tibble of class \code{c("sales_data", ...)} with the raw columns plus,
when the corresponding files are supplied, store-metadata columns and the
logical \code{is_holiday} / \code{holiday_description} columns.
}
\description{
Reads \code{train.csv} with an explicit column specification, optionally filters by
store and/or family \strong{before} any joins (to save memory on the ~3M-row file),
and optionally enriches the data with store metadata and holiday flags.
}
\details{
Performance: the column types are declared explicitly via
\code{\link[readr:cols]{readr::cols()}} so \code{readr} does not waste time guessing, and the \code{stores} /
\code{families} filters are applied immediately after reading (before the joins) to
shrink the working set as early as possible.

Holiday logic: a date is treated as a day off for a given store according to
its scope -- \strong{National} holidays apply to every store, \strong{Regional} holidays
to stores whose \code{state} matches \code{locale_name}, and \strong{Local} holidays to
stores whose \code{city} matches \code{locale_name}. Rows with \code{transferred == TRUE} are
excluded because the holiday was moved to another date. Regional and local
matching require store metadata, so pass \code{stores_path} together with
\code{holidays_path} for full coverage; otherwise only national holidays are
flagged.
}
\examples{
train <- system.file("extdata", "train_sample.csv", package = "salesTS")
stores <- system.file("extdata", "stores.csv", package = "salesTS")
holidays <- system.file("extdata", "holidays_events.csv", package = "salesTS")

# Filter to a couple of stores/families while reading.
df <- load_sales_data(
  train, stores, holidays,
  stores = 1:3,
  families = c("BEVERAGES", "PRODUCE")
)
df

}
