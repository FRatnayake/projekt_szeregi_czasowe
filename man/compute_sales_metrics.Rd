% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_sales_metrics.R
\name{compute_sales_metrics}
\alias{compute_sales_metrics}
\title{Compute per-group sales metrics}
\usage{
compute_sales_metrics(data, group_by = c("store_nbr", "family"), window = 7L)
}
\arguments{
\item{data}{A sales tibble. Must contain \code{date}, \code{sales}, \code{onpromotion} plus
any columns named in \code{group_by}.}

\item{group_by}{Character vector of grouping columns, any subset of
\verb{\{store_nbr, family, city, state, type, cluster\}}.}

\item{window}{Integer window for the trailing moving average (default \code{7}).}
}
\value{
A tidy tibble with one row per group and the following columns:
\describe{
\item{\code{total_sales}}{Total sales.}
\item{\code{mean_sales}}{Mean daily sales.}
\item{\code{moving_avg_last}}{Last value of the trailing \code{window}-day moving average.}
\item{\code{volatility_sd}}{Standard deviation of sales.}
\item{\code{volatility_cv}}{Coefficient of variation (\code{sd / mean}).}
\item{\code{promo_share}}{Percentage of rows with \code{onpromotion > 0}.}
\item{\code{peak_distance_days}}{Mean spacing between local sales peaks.}
\item{\code{promo_uplift_pct}}{Percentage uplift of mean sales on promo days vs non-promo.}
\item{\code{growth_pop}}{Period-over-period growth (last vs previous month), in percent.}
\item{\code{cagr}}{Compound annual growth rate over the series, in percent.}
\item{\code{trend_slope}}{Slope of \code{lm(sales ~ as.numeric(date))}.}
\item{\code{zero_sales_rate}}{Share of days with \code{sales == 0} (stockout proxy).}
\item{\code{holiday_uplift_pct}}{Percentage uplift on holidays vs non-holidays
(\code{NA} when \code{is_holiday} is absent).}
}
}
\description{
Aggregates a sales table into one tidy row per group, computing a rich set of
descriptive, volatility, promotion, growth and (optionally) holiday metrics.
}
\details{
Peaks are found with an internal helper that flags points strictly
greater than their neighbours within a \code{min_gap} window; \code{peak_distance_days}
is the mean gap between consecutive peaks (\code{NA} with fewer than two peaks).
}
\examples{
compute_sales_metrics(sample_sales, group_by = c("store_nbr", "family"))

# Group by an arbitrary metadata column after a join.
joined <- dplyr::left_join(sample_sales, sample_stores, by = "store_nbr")
compute_sales_metrics(joined, group_by = "type")

}
