% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_sales_trends.R
\name{plot_sales_trends}
\alias{plot_sales_trends}
\title{Visualise retail sales trends}
\usage{
plot_sales_trends(
  data,
  group_by = NULL,
  type = c("line", "decomposition", "heatmap", "promo", "holiday"),
  interactive = FALSE
)
}
\arguments{
\item{data}{A sales tibble with at least \code{date}, \code{sales}, \code{onpromotion}.}

\item{group_by}{Optional character vector of columns used to colour/facet the
\code{"line"} plot (e.g. \code{"family"}).}

\item{type}{One of \code{"line"}, \code{"decomposition"}, \code{"heatmap"}, \code{"promo"},
\code{"holiday"}.}

\item{interactive}{Logical; if \code{TRUE} and the suggested package
\pkg{plotly} is installed, the plot is returned as an interactive
\pkg{plotly} object. Otherwise a warning is emitted and a static
\pkg{ggplot2} object is returned.}
}
\value{
A \pkg{ggplot2} object (or a \pkg{plotly} object when
\code{interactive = TRUE} and \pkg{plotly} is available).
}
\description{
A single entry point for the most useful sales visualisations: time series with
a moving average and promotion/holiday markers, an STL decomposition, a
weekly/yearly seasonality heatmap, a promotion-effect plot and a holiday-effect
plot.
}
\details{
\itemize{
\item \code{"line"} overlays a 7-day moving average, marks promotion days with a rug and
(when \code{is_holiday} is present) holidays with dashed vertical lines.
\item \code{"decomposition"} aggregates everything to one daily series, regularises it
and runs \code{\link[stats:stl]{stats::stl()}}; it errors if the series is shorter than two seasonal
cycles.
\item \code{"heatmap"} shows mean sales over day-of-week x ISO week-of-year.
\item \code{"promo"} compares the sales distribution on promotion vs non-promotion days.
\item \code{"holiday"} requires \code{is_holiday} and compares holiday vs working days.
}
}
\examples{
# Assign to a variable (the plot renders only when printed).
p_line <- plot_sales_trends(sample_sales, type = "line")
p_heat <- plot_sales_trends(sample_sales, type = "heatmap")
p_promo <- plot_sales_trends(sample_sales, type = "promo", group_by = "family")
\dontrun{
print(p_line)
}

}
