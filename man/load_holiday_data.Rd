% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_holiday_data.R
\name{load_holiday_data}
\alias{load_holiday_data}
\alias{summary.holiday_data}
\title{Load and parse a holidays / events file}
\usage{
load_holiday_data(holidays_path)

\method{summary}{holiday_data}(object, ...)
}
\arguments{
\item{holidays_path}{Path to \code{holidays_events.csv}.}

\item{object}{A \code{holiday_data} object.}

\item{...}{Unused; for S3 compatibility.}
}
\value{
A tibble of class \code{c("holiday_data", "tbl_df", "tbl", "data.frame")}
with columns \code{date} (\code{Date}), \code{type}, \code{locale}, \code{locale_name},
\code{description} and \code{transferred} (\code{logical}).
}
\description{
Reads a \code{holidays_events.csv} file (Favorita schema), parses the date,
converts the \code{transferred} text column to a logical, and returns a tibble with
the S3 class \code{"holiday_data"} (which has a Polish-language \code{\link[=summary]{summary()}} method).
}
\details{
The \code{transferred} flag is important and easy to misread: when
\code{transferred == TRUE} the nominal holiday on that date was \strong{moved}, so that
calendar day is \strong{not} a day off. The real day off appears on a different
date, recorded as a \code{Transfer} (or \code{Bridge}) type row. This is why
\code{\link[=load_sales_data]{load_sales_data()}} excludes \code{transferred == TRUE} rows when building the
\code{is_holiday} flag.
}
\examples{
path <- system.file("extdata", "holidays_events.csv", package = "salesTS")
hd <- load_holiday_data(path)
summary(hd)

}
